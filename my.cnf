[mysqld]
max_allowed_packet = 64M

# No need to define, log errors to stdout by default.
# log_error = /var/log/mysql/error.log

slow_query_log = 1
slow_query_log_file = /var/log/mysql/log-slow-queries.log

